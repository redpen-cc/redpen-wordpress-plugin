<?php
global $redpen_base_url;

# The URL of RedPen Server, must be accessible from the same machine, where WordPress is running, not the browser.
# http://localhost:8080/ is a reasonable default if you haven't changed RedPen Server's configuration.
# It will be accessed using proxy.php, which is included with the plugin.

$redpen_base_url = 'http://localhost:8080/';
?>
